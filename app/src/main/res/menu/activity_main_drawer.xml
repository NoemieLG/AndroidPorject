<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
    <item android:title="Matchs">
        <menu>
            <item
                android:id="@+id/add_match"
                android:icon="@drawable/ic_menu_manage"
                android:title="Ajouter un match" />

            <item
                android:id="@+id/see_match"
                android:icon="@drawable/ic_menu_gallery"
                android:title="Voir les anciens matchs" />
        </menu>
    </item>
    <item android:title="Photos">
        <menu>
            <item
                android:id="@+id/nav_camera"
                android:icon="@drawable/ic_menu_camera"
                android:title="Prendre une photo" />
            <item
                android:id="@+id/nav_gallery"
                android:icon="@drawable/ic_menu_gallery"
                android:title="Voir toutes les photos" />
        </menu>

    </item>
    </group>

    <item android:title="Communicate">
        <menu>
            <item
                android:id="@+id/nav_share"
                android:icon="@drawable/ic_menu_share"
                android:title="Share" />
            <item
                android:id="@+id/nav_send"
                android:icon="@drawable/ic_menu_send"
                android:title="Send" />
        </menu>
    </item>

</menu>
